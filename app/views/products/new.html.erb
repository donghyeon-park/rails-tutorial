<h1>New product</h1>

<%= form_with model: @product do |form| %>
  <div>
    <%= form.label :name %>: <%= form.text_field :name %>
  </div>

  <p>
    <%= form.submit %>
  </p>

<% end %>

<%= link_to "Cancel", products_path %>