<h1>Products</h1>

<div id="products">
  <% @products.each do |product| %>
    <div>
      <a href="products/<%= product.id %>">
        <%= product.id %>번 제품: <%= product.name %>
      </a>
    </div>
  <% end %>
</div>